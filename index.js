const rest = require('./lib/rest.js');
const api = require('./lib/api.js');
const common = require('./lib/common.js');

module.exports = {
  aaa: '123456',
  rest: rest,
  api: api,
  common: common,
}
